
GrTracking('importScript', 'ec');
GrTracking(
    'viewCategory',
    {
        "shop": {
            "id": "{$shop_id}"
        },
        "id": "{$category.id}",
        "name": "{$category.name}"
    }
);